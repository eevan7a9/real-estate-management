export enum SocketNotificationType {
  Activity = 'ACTIVITY',
  Enquiry = 'ENQUIRY',
  Logout = 'USER_LOGOUT',
  User = 'USER'
}

export enum UserNotificationType {
  Account = "ACCOUNT", 
  Enquiry = "ENQUIRY", 
  Property = "PROPERTY", 
  System = "SYSTEM"
}